"""Cluster ontology typing subsystem.

The package consumes an already-built ``networkx.DiGraph`` ontology DAG.
It does not parse Turtle/RDF and does not know who created the graph.

Import concrete APIs from their modules, for example:
    from cluster_typing.ontology_probability_scoring import export_ontology_evidence_jsonls
    from cluster_typing.ontology_annotator import annotate_doc_with_ontology_folder
    from cluster_typing.ontology_schema import require_ontology_layer
"""
